server:
  port: 8082

spring:
  application:
    name: api-gateway

  datasource:
    url: jdbc:mysql://localhost:3306/nexthood
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
