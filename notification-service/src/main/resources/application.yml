server:
  port: 8085

spring:
  application:
    name: notification-service

  datasource:
    url: jdbc:mysql://localhost:3306/nexthood
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
